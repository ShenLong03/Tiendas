@model IEnumerable<Domain.DetalleVentas>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Productos.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ventas.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descuento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Productos.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ventas.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descuento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DetalleVentasId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DetalleVentasId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DetalleVentasId })
        </td>
    </tr>
}

</table>
