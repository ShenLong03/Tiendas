@model IEnumerable<Domain.Pagos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MediosPago.FormaPago)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ventas.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MediosPago.FormaPago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ventas.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PagoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PagoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PagoId })
        </td>
    </tr>
}

</table>
